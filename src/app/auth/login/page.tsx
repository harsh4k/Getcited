import { OAuthCard } from "@/components/auth/OAuthCard";

export default function LoginPage() {
  return <OAuthCard heading="Welcome back" subheading="Sign in to run audits and track your AI visibility" />;
}
