import { OAuthCard } from "@/components/auth/OAuthCard";

export default function RegisterPage() {
  return <OAuthCard heading="Create your account" subheading="One click with Google — no forms, no passwords" />;
}
